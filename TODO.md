# V1 follow-ups

- Review projection overlay behavior for very large or off-canvas vectors and improve overflow labeling if needed.
- Add a small in-place example gallery only if the current quick scenes need richer descriptions.
- Consider stable `useCallback` action references if the state tree grows.
